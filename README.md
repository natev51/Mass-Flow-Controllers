# Mass-Flow-Controllers

note that on the front panel, the `Working Gas` is captioned as the control for `Desired Gas` this is a naming issue that should be corrected in the future for easier readability.